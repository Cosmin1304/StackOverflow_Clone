# === TEMPLATE pentru secrets.properties ===
# Copiaza acest fisier ca "secrets.properties" si completeaza valorile reale.
# secrets.properties este in .gitignore (NU se urca pe GitHub); acest .example SE urca.

# Gmail (foloseste un App Password, NU parola contului)
spring.mail.username=
spring.mail.password=

# Twilio (din consola Twilio)
twilio.account-sid=
twilio.auth-token=
