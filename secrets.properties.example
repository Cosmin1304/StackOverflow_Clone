# Copiaza ca secrets.properties si completeaza valorile reale (gitignored).

# Gmail: foloseste App Password, NU parola contului.
spring.mail.username=
spring.mail.password=

twilio.account-sid=
twilio.auth-token=
